server:
  port: 8092
  servlet:
    context-path: /
spring:
  mvc:
    favicon:
      enabled: true
  thymeleaf:
    #缓冲的配置
    cache: false
    encoding: utf-8
    prefix: classpath:/templates
    suffix: .html

logging:
  level:
    com.ly.mapper: debug